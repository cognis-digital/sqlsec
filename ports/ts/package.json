{
  "name": "@cognis-digital/sqlsec",
  "version": "1.0.0",
  "description": "TypeScript port of the core sqlsec passive SQL-safety check (Cognis Digital).",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "license": "LicenseRef-COCL-1.0"
}
